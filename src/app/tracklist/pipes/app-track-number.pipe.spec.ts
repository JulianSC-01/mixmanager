import { AppTrackNumberPipe } from './app-track-number.pipe';

describe('AppTrackNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new AppTrackNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
