import { AppTrackLengthAccessiblePipe } from './app-track-length-accessible.pipe';

describe('AppTrackLengthAccessiblePipe', () => {
  it('create an instance', () => {
    const pipe = new AppTrackLengthAccessiblePipe();
    expect(pipe).toBeTruthy();
  });
});
